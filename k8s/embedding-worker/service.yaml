apiVersion: v1
kind: Service
metadata:
  name: vat-embedding-service
  namespace: vat-system
  labels:
    app.kubernetes.io/name: vat-embedding-service
    app.kubernetes.io/component: inference
    app.kubernetes.io/part-of: vat-enterprise
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vat-embedding-worker
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
